# PROJECT_PROGRESS.md

## Milestones
- Initial add-on plugin bootstrap implemented with provider and entitlement-provider registration.
- Hidden direct-access wizard page, REST routes, capability registration, and add-on settings implemented.
- Project creation service implemented against Coordina's public platform contracts.
- Template catalog normalized into add-on JSON and loaded through a dedicated service.
- Wizard UI rebuilt as a multi-step template-driven flow.
- Standalone repo packaging files and memory docs added.

## Current Status
- Version `0.2.0` is the current add-on baseline.
- The plugin is ready to be split into its own VS Code project and GitHub repository.
- Runtime verification in live WordPress is still pending.

## Next Step
Activate the add-on with Coordina core in WordPress and verify the end-to-end wizard creation flow.